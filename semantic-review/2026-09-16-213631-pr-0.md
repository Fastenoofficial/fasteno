# Fasteno-only storefront and resource identity

The uncommitted change completes the customer-facing rebrand from “Fasteno Shyama” and display-style “Fasteno.in” to “Fasteno.” It routes shared metadata and email branding through `SITE_NAME`, updates remaining hard-coded copy, and synchronizes package and generated-asset identity. It keeps `fasteno.in` where it is an actual domain or mailbox, hides unverified social profiles, and retires an obsolete deployment helper instead of inventing provider URLs. Legal identity and commerce behavior remain unchanged apart from the intended Razorpay display labels.

Watch for: None; no actionable issues were found in the uncommitted diff.

**Verdict**: APPROVED

## High-level view

The visible identity now resolves to Fasteno across metadata, auth/account/admin copy, contact templates, invoice and maintenance output, navigation accessibility text, email sender/header/subjects, Razorpay checkout labels, package metadata, and generated gift-set artwork. `SITE_URL` remains independent, so the display-name change does not rename operational URLs.

The legal entity, registered address, GSTIN, seller state, support contacts, tax rate, shipping thresholds, COD limit, Razorpay amount/order/currency, invoice calculations, and fulfillment paths are unchanged. Only the invoice brand heading and Razorpay’s customer-visible merchant name and description change.

The footer removes rather than redirects the unverified `fastenoshyama` socials. Historical provider identifiers and applied migrations retain their factual names, the cutover runbook reserves `fasteno` for the future new-account project, and package/generator outputs are synchronized; the production build passes.

<details>
<summary>Issues (0)</summary>

No actionable issues.

</details>

<details>
<summary>Details</summary>

## Fasteno display identity remains separate from operational URLs

Changing `SITE_NAME` from `Fasteno.in` to `Fasteno` updates its display-only consumers, including policy/guide copy, root and Open Graph metadata, generated social imagery, and order email branding. Root metadata now uses that shared value instead of separate literals, while `SITE_URL` continues to supply callback, canonical, order, admin, and tracking URLs.

Explicit old-name copy is replaced across auth, account, admin customer-contact templates, shop/search metadata, the craftsmanship band, WhatsApp greeting, navbar/footer alternatives, invoice output, and maintenance HTML. The referenced PNG logo itself reads `FASTENO` with `EXCLUSIVE ACCESSORIES`. Active application code contains no `Fasteno Shyama`, `fastenoshyama`, or display `Fasteno.in`; remaining `fasteno.in` values are real hostnames or mailboxes.

The email shell renders `SITE_NAME.toUpperCase()`, and confirmation, shipped, and delivered subjects append `SITE_NAME`. Customer and owner sends default to `Fasteno <orders@fasteno.in>`, matching the environment example; the owner-alert subject contains no obsolete brand.

## Razorpay and invoice presentation leave commerce state intact

The Razorpay checkout options change only `name` and `description`. Order ID, amount, currency, prefill data, theme, modal behavior, verification, and server-side payment flows receive no diff.

The printable invoice changes only its brand heading to `FASTENO`; seller data still comes from the canonical legal constants through `lib/invoice.ts`. `LEGAL_ENTITY_NAME` remains `SR Creation (Proprietor: Sandeep Agarwal)`, the Jaipur registered address and `08CVGPA9527D1ZO` GSTIN are unchanged, and support remains `care@fasteno.in` / `+91 78784 38958`. GST and interstate/intrastate inputs, prices, shipping rules, and COD limits are untouched.

## Unverified social identity fails closed without rewriting history

The footer deletes all four `fastenoshyama` destinations, their icon imports, and the rendered list. No Instagram, Facebook, X/Twitter, or YouTube destination remains in active source, so no guessed Fasteno handle is exposed.

The GitHub/Vercel helper is disabled rather than rewritten to a guessed repository or project URL. The factual legacy `origin` and historical deployment records remain under `fasteno-shyama`, while operational `fasteno.in` hostnames and addresses remain intact. The production runbook distinguishes the existing domain-owning project from a future new-account destination named `fasteno`; it does not relabel an existing provider resource as that destination.

Applied migrations `001`, `002`, and `006` retain their original “Fasteno Shyama” headers. Only the current aggregate bootstrap and optional seed headers are renamed, avoiding a fabricated rewrite of applied migration history.

## Package and generated assets stay reproducible

`package.json` and the lockfile root record both use `fasteno`; no dependency, script, version, override, or install behavior changes. The gift-set generator and the Boardroom, Heir Wedding, and Monarch SVG outputs all use `FASTENO`, and no residual old brand exists under `public` or `scripts`.

`git diff --check HEAD` completed without errors. `npm ls --depth=0` resolved `fasteno@1.0.0`, and `npm run build` compiled, type-checked, generated all 76 static pages, and completed route optimization, covering the removed social imports and metadata interpolation.

</details>

<details>
<summary>File map (40 files)</summary>

- `.env.example` — updates the example Resend display name and sender domain.
- `AGENT_PLAN.md` — renames the build plan’s project heading.
- `BUILD_LOG.md` — updates the project summary and locked brand decision.
- `PRD.md` — updates the product title and overview brand.
- `PRODUCTION_CUTOVER_PLAN.md` — separates historical projects from the future `fasteno` destination.
- `README.md` — renames the primary project heading.
- `ROADMAP.md` — renames the roadmap heading.
- `app/(auth)/forgot-password/page.tsx` — updates password-reset metadata.
- `app/(auth)/login/page.tsx` — updates sign-in metadata.
- `app/(auth)/register/page.tsx` — updates registration metadata.
- `app/(auth)/reset-password/page.tsx` — updates reset metadata.
- `app/account/layout.tsx` — updates account title templates.
- `app/account/orders/page.tsx` — updates order-history metadata.
- `app/account/page.tsx` — updates profile metadata.
- `app/admin/layout.tsx` — updates admin title templates.
- `app/admin/orders/[id]/page.tsx` — updates admin email and WhatsApp customer templates.
- `app/layout.tsx` — centralizes root titles and Open Graph naming on `SITE_NAME`.
- `app/order/[id]/invoice/page.tsx` — updates invoice metadata.
- `app/order/[id]/page.tsx` — updates order-confirmation metadata.
- `app/search/page.tsx` — updates search metadata.
- `app/shop/page.tsx` — updates catalog metadata.
- `components/account/LoginForm.tsx` — updates registration prompt copy.
- `components/checkout/InvoiceView.tsx` — updates the printed brand heading.
- `components/checkout/razorpay-client.ts` — updates checkout merchant presentation.
- `components/home/CraftsmanshipBand.tsx` — updates brand-story copy.
- `components/layout/Footer.tsx` — updates logo text and removes unverified socials.
- `components/layout/Navbar.tsx` — updates logo accessibility text.
- `components/layout/WhatsAppButton.tsx` — updates the support greeting template.
- `deploy-via-github.bat` — retires obsolete provider deployment instructions.
- `lib/config.ts` — changes the canonical display name and grievance-officer label.
- `lib/email.ts` — changes the default sender display name.
- `middleware.ts` — updates maintenance-page branding.
- `package-lock.json` — synchronizes the root package identity.
- `package.json` — renames the private package.
- `public/products/the-boardroom-set.svg` — updates generated box branding.
- `public/products/the-heir-wedding-set.svg` — updates generated box branding.
- `public/products/the-monarch-gift-set.svg` — updates generated box branding.
- `scripts/generate-images.mjs` — updates future gift-set generation.
- `supabase/SETUP_ALL.sql` — updates current bootstrap/seed headers.
- `supabase/seed.sql` — updates the optional seed header.

Full diff: `git diff HEAD`

</details>
