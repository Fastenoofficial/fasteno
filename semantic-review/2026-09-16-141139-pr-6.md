# Exact migration-006 fingerprint with fail-closed branch selection

The final runbook correction closes the remaining ambiguity in deciding whether production already matches migration 006. It captures relation persistence, complete guest/security table column metadata, and an expanded sequence ACL exposing `USAGE`, `SELECT`, and `UPDATE` for `PUBLIC` and every application role. The strict criterion routes any missing evidence or mismatch to protected partial-006 convergence, while the earlier release, maintenance, webhook, cron, ordering, and rollback safeguards remain intact. **Watch for:** **confirmed** operational NO-GO — no live database/provider outputs are present, so this approval validates the plan only and does not authorize cutover.

**Verdict**: APPROVED

## High-level view

The relation projection now includes `relpersistence`; the two-table column inventory returns every column in ordinal order with type identity, nullability, identity/generated state, generation expression, and default expression. This supports direct comparison with migration 006 rather than inference from constraints.

The sequence query expands the stored ACL and maps grantee OID zero to `PUBLIC`. It enumerates any direct `USAGE`, `SELECT`, and `UPDATE` grants held by `PUBLIC`, `anon`, `authenticated`, or `service_role`, including grantability and grantor.

Branch A still requires a trusted ledger and an exact definition/ACL match. Any missing output or mismatch is explicitly partial 006 and enters Branch B under maintenance; the opening NO-GO and mandatory gates keep unavailable live evidence blocking.

<details>
<summary>Issues (0)</summary>

No actionable plan defects.

</details>

<details>
<summary>Details</summary>

## Relation and column evidence covers the 006 table contract

The corrected catalog query (`PRODUCTION_CUTOVER_PLAN.md:121-142`) exposes `relpersistence` with relation kind, RLS state, ACL, and ownership, so an unlogged substitute cannot satisfy the permanent-table fingerprint. The column result covers every field of `guest_order_access_tokens` and `security_migration_state`, including UUID/text/timestamptz identity, required-versus-nullable state, generated/identity status, and the `gen_random_uuid()`/`now()` defaults. Separate constraint and index results retain the key, foreign-key, check, uniqueness, and partial-index evidence.

## Expanded order-number sequence ACL matches the grant contract

Migration 006 grants `USAGE`, `SELECT`, and `UPDATE` on `order_number_seq` to `service_role`, with none granted to `PUBLIC`, `anon`, or `authenticated`. The corrected `aclexplode` query (`PRODUCTION_CUTOVER_PLAN.md:180-203`) exposes all three service-role rows as well as any unexpected public/client-role row; a missing service privilege is likewise visible by comparison. This closes the prior `information_schema.usage_privileges` blind spot for sequence `SELECT` and `UPDATE`.

## Strict classification and prior safeguards remain fail-closed

The criterion at `PRODUCTION_CUTOVER_PLAN.md:233` classifies any missing ledger, definition, constraint, RLS flag, `PUBLIC`/role/sequence privilege, function configuration, policy expression, or other mismatch as partial 006. Branch B keeps traffic and writers blocked, reviews the mismatch, forward-converges 006, and repeats exact ACL/definition and negative-probe checks before later migrations.

The prior safety corrections remain present: one immutable release commit is mandatory; every production-credentialed alias and immutable URL must be protected and anonymously probed; Razorpay needs a rehearsed signed-webhook/operator path; fallback cron authorization stays absent or disabled until source authorization is removed; maintenance encloses traffic movement and migrations; and rollback is schema-aware.

**Confirmed operational limitation:** no live Supabase fingerprint, trusted ledger, ownership proof, backup/restore result, provider configuration, protection probe, or approval is supplied. `Status: NO-GO`, the unchecked mandatory gates, and explicit approvals keep all of them launch-blocking despite the plan verdict.

</details>

<details>
<summary>File map</summary>

- `PRODUCTION_CUTOVER_PLAN.md` — completes migration-006 relation/column/sequence evidence while retaining fail-closed classification and cutover safeguards.

The plan is untracked; full diff: `git diff --no-index -- NUL PRODUCTION_CUTOVER_PLAN.md`.

</details>
