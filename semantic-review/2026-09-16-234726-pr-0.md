# Recovered production catalog with local placeholder media

The change reconstructs the approved production catalog after the old Supabase project was deleted, using slug-keyed upserts while leaving old row identities and unavailable Storage URLs behind. It restores six categories and twelve products from preserved final-build payloads, assigns deterministic positions to the ten recovered featured products, and substitutes twenty-four local illustrated SVGs for the lost binaries. Migration 012 carries forward the separately approved advisor hardening, with an exact local Supabase CLI pin supporting the migration workflow.

Watch for: None; no actionable finding remains.

**Verdict**: APPROVED

## High-level view

Migration 011 contains only the approved six category and twelve product slugs. Recovered category metadata and all product catalog fields match the preserved final-build payloads; only project-bound IDs and deleted Storage images are intentionally replaced.

All eighteen records upsert by unique slug. Product upserts first leave every recovered row outside the partial featured-order index, then one complete-set update restores ten featured members at unique positions `0–9`; both a clean application and exact rerun are collision-free under the migration-008 indexes.

Each product references two tracked local SVGs. All twenty-four are self-contained 800×1000 images with linked accessible labels, explicit temporary-illustration wording, no active or remote content, and successful rasterization.

Migration 012 is unchanged from its prior approved semantic pass. The README accurately describes its reviewed role boundaries and the helper/admin-definer advisor findings intentionally left behind.

The npm manifest and lockfile pin the official `supabase` CLI exactly at `2.117.0` as development tooling, including matching optional platform binaries. Ignoring `supabase/.temp/` excludes generated local state without hiding migrations or configuration.

<details>
<summary>Issues (0)</summary>

No actionable findings.

</details>

<details>
<summary>Details</summary>

## Catalog recovery without legacy project identity

Migration 011’s category names and descriptions match the six rendered recovery pages in the approved `ties`, `cufflinks`, `brooches`, `pocket-squares`, `buttons`, and `gift-sets` order. Sort values are `1–6`, category images remain null, and all six remain home-visible.

All twelve product payloads were compared field-for-field. Names, descriptions, detail arrays, ₹999.00 prices, null compare-at prices, stock `100`, active state, category, material, color, pattern, tags, timestamps, and ten-versus-two featured membership match the recovered records; origin remains India and empty HSN/SEO values are preserved. The SQL mutates only `public.categories` and `public.products` and contains no UUID literal, email, phone number, external URL, or statement against customer, profile, address, wishlist, review, order, request, or subscriber data. Category relationships and conflicts resolve by slug, so neither old product UUIDs nor old `jnvhnfjpeaifclajacsm.supabase.co` image URLs cross from the recovery payloads into the destination.

## Featured membership staged around unique positions

Migration 008 makes category positions globally unique and featured positions unique within `featured IS TRUE`. Every 011 product upsert stages the row as unfeatured at order zero, where duplicate zeroes are outside that partial index; the final update leaves the two originally nonfeatured products neutral and gives the ten recovered featured products a one-to-one `0–9` mapping in recovered storefront order.

All six category and twelve product writes use `ON CONFLICT (slug) DO UPDATE`. On an exact rerun, category values stay unchanged, product upserts remove the ten rows from featured-index membership, and the final update reconstructs the unique set. A first-run/rerun state model completed without either ordering collision.

## Temporary media is local, renderable, and disclosed

Every `images` array contains the main/detail pair derived from its slug, and all paths resolve under `public/products`. XML parsing confirmed the SVG namespace and structure; each root has `role="img"` and `aria-labelledby="title desc"`, each title matches its product, and every description says `Temporary Fasteno illustrated product image`. No file contains scripts, foreign objects, event handlers, or remote references, and Sharp decoded and rasterized all twenty-four.

The migration header and README explain that deletion of the old project removed the photography, identify these files as temporary local illustrations, and direct operators to replace them from the admin before making product-photography claims.

## Advisor hardening remains within approved trust boundaries

The earlier migration-012 review traced signup, anonymous ratings, coupon validation, admin catalog operations, owner commerce reads, banner scheduling, and settings upserts and returned APPROVED. Direct trigger-function invocation remains removed without detaching signup; ratings remain caller-rights and approved-only; helpers and nine admin RPCs retain only their required roles; and admin definer RPCs retain fixed search paths and in-body administrator checks.

The RLS rewrites retain their command scopes and owner/admin alternatives while caching auth helpers. Banner reads remain the scheduled-public/all-row-admin union, banner and settings writes remain admin-only, and six indexes cover previously unserved foreign-key directions. The README therefore correctly presents the helper functions as intentionally role-exposed and the authenticated definer mutators as body-checked residuals rather than claiming a warning-free advisor result.

## Exact local CLI dependency and generated-state boundary

`package.json` adds only `supabase: "2.117.0"` under `devDependencies`. The lock root, resolved package, and eight optional OS/architecture binaries use that exact version; npm adds the expected `eciesjs`, `jose`, and noble-crypto chain. `npm ls` resolves one non-overridden instance, the executable reports `2.117.0`, and a lock-only dry run is stable. Keeping migration tooling out of runtime dependencies and ignoring only its `.temp` state are the appropriate boundaries.

## Static verification without database mutation

Validation covered recovery-payload equality, mutation/sensitive-literal scans, first-run/rerun uniqueness modeling, XML/accessibility checks, all-asset rasterization, `git diff --check HEAD`, npm resolution, CLI version execution, and an npm lock-only dry run. Not tested: applying 011 or 012 to a local or linked database, as instructed; SQL behavior was instead traced against migrations 001–010 and their slug and ordering indexes.

</details>

<details>
<summary>File map</summary>

- `supabase/migrations/011_catalog_transfer.sql` — restores only the approved catalog with destination-local identity and deterministic featured order.
- `supabase/migrations/012_advisor_hardening.sql` — retains the previously approved function, RLS, policy, and reverse-FK advisor hardening.
- `supabase/README.md` — documents migration order, production-versus-demo catalog use, placeholder provenance, and intentional advisor residuals.
- `.gitignore` — excludes Supabase CLI temporary state.
- `package.json` — adds the exact Supabase CLI development pin.
- `package-lock.json` — locks the CLI, matching platform packages, and expected transitive dependencies.
- `public/products/contemporary-jacquard-silk-tie-muted-teal-mint-geometric-stripe{,-detail}.svg` — accessible temporary main/detail illustrations.
- `public/products/baroque-crimson-onyx-jacquard-silk-tie-ombr-panel-edition{,-detail}.svg` — accessible temporary main/detail illustrations.
- `public/products/modern-baroque-silk-tie-royal-blue-silver-ice-stripe-edition{,-detail}.svg` — accessible temporary main/detail illustrations.
- `public/products/asymmetrical-embroidered-effect-paisley-tie-rich-wine-icy-grey{,-detail}.svg` — accessible temporary main/detail illustrations.
- `public/products/architectural-herringbone-micro-grid-jacquard-tie-monochrome-graphite{,-detail}.svg` — accessible temporary main/detail illustrations.
- `public/products/contemporary-fragmented-block-tie-muted-burgundy-charcoal{,-detail}.svg` — accessible temporary main/detail illustrations.
- `public/products/monochrome-paisley-jacquard-tie-slate-grey-matte-black-panel-edition{,-detail}.svg` — accessible temporary main/detail illustrations.
- `public/products/modern-panel-silk-tie-ruby-red-damask-with-diagonal-accent-stripe{,-detail}.svg` — accessible temporary main/detail illustrations.
- `public/products/architectural-floral-silk-tie-shaded-slate-silver-parquet-weave{,-detail}.svg` — accessible temporary main/detail illustrations.
- `public/products/tailored-baroque-paisley-tie-rich-burgundy-with-dual-tone-textured-panels{,-detail}.svg` — accessible temporary main/detail illustrations.
- `public/products/contemporary-panel-jacquard-tie-seafoam-ice-mint-steel-blue{,-detail}.svg` — accessible temporary main/detail illustrations.
- `public/products/asymmetrical-jacquard-paisley-tie-muted-burgundy-silver-lace-edition{,-detail}.svg` — accessible temporary main/detail illustrations.

Full tracked diff: `git diff HEAD`. Untracked migrations and assets: `git diff --no-index -- /dev/null <path>` for each path listed by `git ls-files --others --exclude-standard`.

</details>
