/** Active catalog products at or below this quantity need attention. */
export const LOW_STOCK_THRESHOLD = 5;

/** Keep one bulk request and its immutable activity row reasonably bounded. */
export const MAX_ADMIN_BULK_PRODUCTS = 500;
